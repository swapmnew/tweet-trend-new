pipeline {
    agent {
      node {
          label 'build-slave'
      }
    }
    stages {
        stage('clone_repo') {
            steps {
                git branch: 'main', url: 'https://github.com/swapmnew/tweet-trend-new.git'
            }
        }
    }
}
